# 作品说明 · 可编辑简历生成器

> 作者：吴嘉欣 ｜ 南京大学 城乡规划 硕士（2028 届）｜ 方向：AI 产品经理
> 作品形态：单文件 HTML 应用（双击即用，离线可用，无需联网/安装）

---

## 一、作品定位

一个**面向求职者的可视化简历编辑器**：左侧调版式与样式，右侧即时预览 A4 成品，内容自动本地保存，一键导出高清 PDF / 图片。既是"简历"，也是一个完整可交付的**轻量产品**。

---

## 二、背景与痛点

市面上的简历工具要么模板僵化、要么导出带水印、要么版式与 Word/PS 对不齐。我需要的不是"再填一张表"，而是一个**自己完全掌控排版、且能随时改、随处打开**的简历资产。

**产品判断**：简历是高频迭代、强个性化的个人资产，最优解是「本地优先 + 所见即所得 + 可导出」，而非依赖某个 SaaS 的存储与模板。

---

## 三、我的角色

产品 + 全栈实现一人完成：需求定义、信息架构、视觉规范、交互设计，以及 HTML/CSS/JS 工程落地。

---

## 四、核心功能（用户视角）

- **所见即所得编辑**：正文 `contenteditable` 直接改，改完即所见。
- **版式自定**：字号 / 字体 / 行距按 5 类文本（姓名 / 意向信息 / 标题 / 正文 / 日期）分别调，颜色支持紫 / 黑 / 灰 + 局部加粗。
- **动态结构**：教育 / 技能 / 实习 / 项目板块可任意增删，照片与校徽可上传。
- **本地持久化**：`localStorage` 实时保存 + 每 5 分钟兜底，关掉再开内容不丢。
- **高清导出**：4 倍分辨率导出 PDF / PNG，远超市面截图清晰度。
- **一键展示模式**：隐藏所有编辑控件，呈现干净成品简历（方便直接演示 / 截图）。

---

## 五、关键设计决策（PM 视角）

| 决策 | 理由 |
|---|---|
| **A4 固定版面（794px）** | 一比一还原打印/导出尺寸，所见即所得，避免"屏幕好看、导出错位" |
| **南大紫 #823A8F 主色** | 个人品牌一致性；紫色标题 / 标签底纹强化记忆点 |
| **5 类文本层级** | 把"样式"抽象成可复用类型，降低用户认知负担、保证全局统一 |
| **本地优先存储** | 数据留在自己机器，隐私可控、零延迟、不依赖账号 |
| **粘贴净化机制** | 从 Word/网页复制进来的脏 DOM 会导致导出崩溃，入库前统一清洗，保证"导得出" |
| **长词折行** | 中英文 / 链接混排时自动折行，杜绝右侧大片留白破坏美观 |

---

## 六、技术实现要点

- **`contenteditable` + `localStorage`**：零后端即可持久化与恢复。
- **`html2canvas` 本地化**：库已随文件夹打包，断网也能导出（CDN 仅作兜底）。
- **导出保真**：导出前内联所有计算样式、强制 100% 缩放、等待字体加载完成，确保屏幕与导出一致。
- **字体对齐**：字体栈以「微软雅黑」优先，与 PS 排版字形一致；头像按原比例绘制不变形。

---

## 七、如何查看与演示

1. **直接打开**：双击 `简历编辑器.html`，首次打开即显示完整示例简历。
2. **展示模式**：点右下角「🖥️ 展示模式」隐藏编辑界面，只看干净成品；再点「✏️ 编辑模式」返回。
3. **导出**：工具栏「导出 PDF / 导出图片」生成高清文件（建议先点一次「保存」）。

> 文件夹已自带 `html2canvas.min.js` 依赖，整体离线可运行，无需联网。

---

## 八、这份作品想说明什么

它不只是一个简历，而是一次完整的**「识别痛点 → 定义需求 → 设计信息架构 → 落地可用产品」**的最小闭环。对一个 AI 产品经理而言，能亲手把"想要的工具"做出来，比描述它更有说服力。
